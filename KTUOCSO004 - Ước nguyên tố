#include<bits/stdc++.h>
using namespace std;
long long checknt(long long n)
{
    long long tich=1;
    if(n<2) return 0;
    for(int i=2;i<=sqrt(n);i++)
    {
        if(n%i==0)
        {
            tich*=i;
            while(n%i==0)
            {
                n/=i;
            }
        }
    }
    if(n>1) tich*=n;
    return tich;
}
int main()
{
   int q;
   cin>>q;
   while(q--)
   {
       long long n;
       cin>>n;
       if(checknt(n)< n)
       {
           cout<<"YES"<<endl;
       }else{cout<<"NO"<<endl;}
   }
}
