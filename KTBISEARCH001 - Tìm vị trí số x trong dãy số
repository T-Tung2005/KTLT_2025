#include<bits/stdc++.h>
using namespace std;
int timkiem(int a[], long long n, long long x)
{
    int l=1,r=n;
    int res=0;
    while(l<=r)
    {
        int mid=(l+r)/2;
        if(a[mid]==x)
        {
            res=mid;
            r=mid-1;
        }else if(a[mid]<x)
        {
            l=mid+1;
        }else
        r=mid-1;
    }
    return res;
}
int main()
{
    int n,m;
    cin>>n>>m;
    int a[n],b[m];
    for(int i=1;i<=n;i++)
    {
        cin>>a[i];
    }
    for(int j=1;j<=m;j++)
    {
        cin>>b[j];
    }
    for(int i=1;i<=m;i++)
    {
        int k=timkiem(a,n,b[i]);
        cout<<k<<" ";
    }
}
